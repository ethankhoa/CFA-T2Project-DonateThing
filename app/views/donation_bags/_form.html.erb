<%= form_for(donation_bag) do |f| %>
  <% if donation_bag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(donation_bag.errors.count, "error") %> prohibited this donation_bag from being saved:</h2>

      <ul>
      <% donation_bag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :bag_status_id %>
    <%= f.text_field :bag_status_id %>
  </div>

  <div class="field">
    <%= f.label :total_quantity %>
    <%= f.number_field :total_quantity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
